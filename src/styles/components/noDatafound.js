import styled, { breakpoints } from '../config'

export const Wrapper = styled.div`
  height: 100%;
  margin: 100px auto;
  display: flex;
  flex-direction: column;
  align-items: center;

  img {
    margin: 15px auto;
  }

  h2 {
    font-weight: bold;
    font-size: 20px;
    line-height: 34px;
    text-align: center;
    color: #7f977b;
  }
`
